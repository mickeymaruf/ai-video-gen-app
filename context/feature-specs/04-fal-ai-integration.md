Integrate the @src/components/editor/scene-editor.tsx   component with fal.ai's image-to-video model (`fal-ai/minimax-video/image-to-video`), use server actions to handle server-side logics do not use api_route handler. When user clicks generate it takes the Script, Visual Guide and Product Images, take a look into how images are being handled inside @src/components/editor/product-image-uploader.tsx , pass it to the action, and initiate an asynchronous queued polling mechanism with live log tracking to prevent UI blocking. Videos must be in 9:16 ratio for now hardcoded. Finally, once the generation is completed, render the `<video>` preview component immediately below the configuration card, matching the existing shadcn, Tailwind, and typography aesthetics perfectly without altering unrelated UI layouts.

Keep the implementation ultra-lean: write highly concise logic with zero unnecessary boilerplate, minimal inline comments, and clean, direct execution that fulfills all technical requirements without over-engineering.