Modify the @src/components/editor/scene-editor.tsx to show the start_card & end_card below product images instead of the right side. Remove the tab switch for start/end card on the right and replace the section for showing the ai generated video output in 9:16 ratio. Reference provided screenshot for the layout.


# Start Card/End Card.
Look into @src/components/editor/product-image-uploader.tsx, we want similiar for drag & drop images for start/end card. Just take it do not apply this into model yet.